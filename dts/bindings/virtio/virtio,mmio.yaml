description: VIRTIO MMIO bindings
compatible: "virtio,mmio"

include: base.yaml

bus: virtio

properties:
    reg:
      required: true
    interrupts:
      required: true
    dma-coherent:
      type: boolean

interrupt-cells:
  - type
  - irq
  - flags
  - priority
